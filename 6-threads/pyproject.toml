[project]
name = "6-threads"
version = "0.1.0"
description = "Post 6 — Conversation state & context management."
requires-python = ">=3.11,<3.14"
dependencies = [
  "openai>=1.0",
  "httpx>=0.27",
  "litellm[proxy]>=1.86",
  "prisma>=0.15.0",
  "psycopg[binary]>=3.2",
]
