[project]
name = "4-auth"
version = "0.1.0"
description = "Post 4 — Auth, API keys, and per-key model scoping."
requires-python = ">=3.11,<3.14"
dependencies = [
  "openai>=1.0",
  "httpx>=0.27",
  "litellm[proxy]>=1.86",
  "prisma>=0.15.0",
]
